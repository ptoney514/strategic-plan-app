'use client'

// NOTE: SystemAdminEditorialLayout uses React Router <Outlet>, will be migrated in Phase 3.
export default function AdminLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
