// Phase 4 Stage 2 placeholder. Replaced in Stage 3 with the moved
// PlanLandingView (verbatim, minus import path updates). Exists now so the
// template registry's lazy import resolves before the move lands.
export default function SidebarLandingView() {
  return (
    <div style={{ padding: "2rem" }}>
      <p>Sidebar tree template — wiring lands in Phase 4 Stage 3.</p>
    </div>
  );
}
